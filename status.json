{
  "state": "experimenting",
  "iteration": 10,
  "northStar": "Determine whether Cloudflare can turn an existing OpenAPI API into a safe native WebMCP interface without changing the API implementation.",
  "proven": [
    "One explicitly enabled OpenAPI GET operation became a native read-only WebMCP tool in Chrome 151.",
    "The native call reached the unchanged fixture API and changed visible page state.",
    "An operation without x-webmcp-enabled true was absent from native discovery and the generated call route.",
    "An external same-origin bootstrap preserves script-src self and passes native discovery and invocation.",
    "Cloudflare can generate a same-origin companion document for a JSON-only API, and that document can publish and call native tools.",
    "Ten tested OpenAPI-style schema shapes registered, were natively discovered, and completed native invocations in Chrome 151.",
    "A native call reached a separately protected unchanged origin while the random origin credential remained only in the edge binding and protected subrequest.",
    "Path encoding, scalar query parameters, JSON bodies, and HTTP error status matched an independent echo origin.",
    "The edge can verify a server-issued HttpOnly browser session independently of tool input.",
    "An edge policy denied a native call before any protected-origin request, while an allowed control incremented the independent origin counter exactly once.",
    "A native write remained pending at zero origin calls, exact separate approval executed it once, and a replay returned 409 without another origin call."
  ],
  "falsified": [
    "Inline script injection works on a page with script-src self.",
    "A JSON API document can itself publish native WebMCP tools.",
    "Chrome registration means Chrome enforces JSON Schema constraints before tool execution.",
    "The current compiler has general OpenAPI request fidelity: array query, header, and multipart cases failed or were unsupported.",
    "Native WebMCP supplies an edge-verifiable agent principal distinct from the browser session."
  ],
  "notProven": [
    "CSP policies that require a nonce or hash and do not allow self",
    "Specification drift",
    "Tool-count limits"
  ],
  "currentExperiment": "08-specification-drift",
  "nextAction": "Observe native tool addition, schema change behavior, and removal while one page remains open."
}
